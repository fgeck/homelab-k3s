---
# yaml-language-server: $schema=https://raw.githubusercontent.com/yannh/kubernetes-json-schema/master/v1.31.0-standalone-strict/secret-v1.json
# Reference to postgresql-vecto-radarr-auth secret for media namespace
# This is a copy of the auth secret from default namespace where CloudNativePG resides
apiVersion: v1
kind: Secret
metadata:
  name: postgresql-vecto-radarr-auth
  namespace: media
type: kubernetes.io/basic-auth
stringData:
  username: ${quote}${DEFAULT_POSTGRES_RADARR_USER}${quote}
  password: ${quote}${DEFAULT_POSTGRES_RADARR_USER_PASSWORD}${quote}
