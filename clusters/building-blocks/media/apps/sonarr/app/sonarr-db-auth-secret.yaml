---
# Reference to postgresql-vecto-sonarr-auth secret for media namespace
# This is a copy of the auth secret from default namespace where CloudNativePG resides
apiVersion: v1
kind: Secret
metadata:
  name: postgresql-vecto-sonarr-auth
  namespace: media
type: kubernetes.io/basic-auth
stringData:
  username: ${quote}${DEFAULT_POSTGRES_SONARR_USER}${quote}
  password: ${quote}${DEFAULT_POSTGRES_SONARR_USER_PASSWORD}${quote}
