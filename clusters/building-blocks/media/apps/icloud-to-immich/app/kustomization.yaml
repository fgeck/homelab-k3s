---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # - ./icloudpd-cronjob.yaml
  - ./immich-go-cronjob.yaml
  - ./icloudpd-secret.yaml
  - ./immich-go-secret.yaml
  - ./icloudpd-config-pvc.yaml
  # kei replaces icloudpd as the download step — enable once tested,
  # then remove icloudpd-* resources and delete the icloudpd-download CronJob from the cluster
  - ./kei-cronjob.yaml
  - ./kei-secret.yaml
  - ./kei-config-pvc.yaml
